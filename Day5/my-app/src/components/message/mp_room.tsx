import "./index.css";
import axios from "axios";
/*
const def_url = 'http://127.0.0.1:8000/api/room/message?roomID=';

type MpRoomProps = {
    room_id: number;
    message: string;
}

function Mp_room(props : MpRoomProps): JSX.Element {
    axios.post(`${def_url}${MpRoomProps.roomID}`, {
        message: MpRoomProps.message
    }).then ((response) => {
        console.log(response);
        return response
    },
        (error) => {
            console.log(error)
            return error;
    });
    return (<div></div>)
}

export default Mp_room*/